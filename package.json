{
  "version": "0.0.0",
  "private": true,
  "name": "FruitOrVegetable",
  "devDependencies": {
    "karma": "^0.12.16",
    "http-server": "^0.6.1",
    "bower": "^1.3.1"
  },
  "scripts": {
    "postinstall": "bower install",

    "prestart": "npm install",
    "start": "http-server -p 8000"
  }
}